# Update local dotfiles
ldfu() {
    declare -f dfu > /dev/null && dfu &&
    (
        cd ~/.dotfiles_local && git pull --ff-only && ./install -q
    )
}

# Aliases
alias ben='ssh anish@ben.csail.mit.edu'
alias jarvis='ssh alarm@jarvis.pdos.csail.mit.edu'
